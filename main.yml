---
- name: install ClamAV
  hosts: lab_core_services
  become: yes
  tasks:

    # install 
    - name: install ClamAV
      ansible.builtin.include_role:
        name: clam-av
      vars:    
        action_type: "install"
