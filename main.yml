---

- name: install ClamAV
  hosts: lab_core_services
  become: yes
  tasks:

  # install ClamAV and Clamd virus scanner
  - name: install ClamAV Virusscaner
    ansible.builtin.include_role:
      name: clam-av
    vars:    
      action: "install"
